# Explain the difference between | , || , & and &&
# |: Regular OR operator, evaluates both operands.
# ||: Short-circuiting OR operator, evaluates the second operand only if necessary.
# &: Regular AND operator, evaluates both operands.
# &&: Short-circuiting AND operator, evaluates the second operand only if necessary.

